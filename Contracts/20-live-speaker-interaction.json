{
  "module": "LiveSpeakerInteraction",
  "scope": "App wiring",
  "inputs": {
    "speaker": "AcousticMate Speaker | null",
    "speakerSet": "AcousticMate SpeakerSet | null",
    "node": "S3D/domains/acoustics/SpeakerNode | reusable set/group manipulation target",
    "fieldView": "S3D/domains/acoustics/SampledFieldPlane | OrthogonalFieldSlices"
  },
  "flow": [
    "global Gizmos enable gates pointer-driven speaker and SpeakerSet transforms",
    "when Gizmos are disabled pointer interaction does not move speakers or sets",
    "the first click selects a manipulation target in Position mode",
    "clicking the selected target without dragging toggles Position and Rotate modes",
    "Position drag changes XZ while Shift-drag changes Y",
    "Rotate drag changes pitch/yaw while Shift-drag changes roll",
    "transform changes update the bound AcousticMate model",
    "SpeakerSet member world transforms are resolved from parent and member-local transforms",
    "affected speaker-mode coupling is invalidated",
    "combined acoustic field is invalidated",
    "visible field samples are recomputed on the next animation frame"
  ],
  "invariants": [
    "speaker and set counts are unrestricted by architecture",
    "each standalone speaker can be moved independently when Gizmos are enabled",
    "direct object dragging is not an alternate movement path when Gizmos are disabled",
    "a SpeakerSet can be manipulated as one parent object in the main visualizer",
    "moving or rotating a SpeakerSet preserves its member-local transforms",
    "individual member geometry is edited in SpeakerSetEditor",
    "speaker height participates in modal coupling",
    "speaker/set orientation participates in directional coupling when directivity data exists",
    "visual feedback continues throughout manipulation",
    "drag end is not a calculation trigger or synchronization barrier",
    "multiple rapid transform events collapse into at most one field rebuild per animation frame",
    "reduced preview resolution is allowed only when full resolution is restored after manipulation"
  ]
}
