{
  "module": "SpeakerModeCoupling",
  "scope": "AcousticMate",
  "instantiable": true,
  "inputs": {
    "speaker": "Speaker",
    "mode": "AcousticMode"
  },
  "outputs": {
    "spatialCoupling": "complex | number",
    "directivityGain": "complex | number",
    "responseGain": "complex",
    "combinedExcitation": "complex"
  },
  "rules": [
    "speaker position affects modal excitation",
    "speaker orientation and DirectivityModel affect coupling when directional data is available",
    "signal-chain phase must be preserved",
    "directivity participates in source coupling rather than being applied only as a post-render visualization effect",
    "SpeakerSet members are coupled as their individual Speaker instances using their resolved world transforms"
  ]
}
