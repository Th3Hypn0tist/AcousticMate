{
  "module": "AcousticMateApp",
  "scope": "App",
  "responsibility": [
    "instantiate S3D modules",
    "instantiate one or more WebGUI runtimes",
    "instantiate AcousticMate domain modules",
    "wire events and data flow",
    "manage project state",
    "compose application UI from WebGUI primitives",
    "load and expose SpeakerModel library data",
    "open and commit RoomEditor workflows",
    "open and commit SpeakerSet editing workflows",
    "instantiate AcousticObject treatment semantics using existing S3D Primitive instances"
  ],
  "mustNot": [
    "contain acoustic equations that belong in domain solvers",
    "duplicate S3D generic geometry functionality",
    "define a second primitive type system in AcousticMate",
    "add AcousticMate concepts to WebGUI",
    "fork or modify reusable S3D domain modules as AcousticMate-owned implementation",
    "mutate module internals outside public contracts",
    "infer absorber or diffuser physics from primitive names",
    "recommend absorber or diffuser placement automatically unless a later explicit contract enables it"
  ],
  "distribution": {
    "layout": {
      "S3D": "vendor/S3D",
      "WebGUI": "vendor/WebGUI"
    },
    "rules": [
      "a single AcousticMate clone contains all browser runtime dependencies",
      "vendored directories are immutable upstream snapshots from the application's perspective",
      "framework changes are made in their owning repositories and then refreshed into vendor"
    ]
  },
  "primaryFlow": [
    "load optional JPG/PNG reference",
    "trace room polygon",
    "add wall and cross-room measurements",
    "solve constrained geometry",
    "extrude room by height",
    "edit room dimensions and boundary openings in the dedicated RoomEditor",
    "treat open doorways as non-reflecting boundary patches without solving exterior space",
    "optionally create physical acoustic-treatment objects using S3D Primitive geometry plus AcousticBoundaryModel semantics",
    "choose or create SpeakerModel definitions",
    "place one or more Speaker instances",
    "optionally group speakers into SpeakerSet instances",
    "edit SpeakerSet member-local positions and orientations in the dedicated SpeakerSetEditor",
    "return to the main visualizer and manipulate complete sets through their parent transforms",
    "bind every visible speaker/set representation to reusable S3D interaction structures",
    "configure each speaker or set signal chain and crossover routing",
    "solve room modes",
    "compute phase-aware combined acoustic field including directional coupling where available",
    "invalidate and resample the visible field continuously while any speaker or set moves",
    "select single frequency or frequency range",
    "visualize field in 2D, 3D or both"
  ]
}
