# Themes

Built-in themes live permanently in this directory. Pass a built-in name such as `default`, an external path, or a `URL` to each `WebGUI` instance. External themes remain owned by the consuming project; WebGUI never copies or mutates them.
