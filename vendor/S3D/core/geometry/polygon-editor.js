import { SceneObject } from '../objects/object.js';
function finite(v,n){v=Number(v);if(!Number.isFinite(v))throw new Error(`${n} must be finite`);return v;}function cloneVertices(v){return v.map(x=>({...x}));}function normalizeVertex(v,id){id=String(v?.id??id??'');if(!id)throw new Error('Polygon vertex requires a non-empty id');return{id,x:finite(v?.x,'Vertex x'),z:finite(v?.z,'Vertex z')}}
function polygonEdges(polygon){const vertices=polygon?.vertices??[],count=vertices.length;if(count<2)return[];const edgeCount=polygon.closed?count:count-1;return Array.from({length:edgeCount},(_,index)=>{const start=vertices[index],end=vertices[(index+1)%count];return{id:`edge:${start.id}:${end.id}`,index,start,end};});}
class PolygonEditor extends SceneObject{constructor({id='polygon-editor',vertices=[],closed=false,y=0,color=[.7,.82,1],selectedColor=[1,.75,.2],metadata={}}={}){super({id,selectable:false,metadata});this.vertices=vertices.map((v,i)=>normalizeVertex(v,`vertex-${i+1}`));if(new Set(this.vertices.map(v=>v.id)).size!==this.vertices.length)throw new Error('Polygon vertex ids must be unique');this.closed=Boolean(closed);this.y=finite(y,'Polygon y');this.color=[...color];this.selectedColor=[...selectedColor];this.selection=new Set();this.sequence=this.vertices.length;this.undoStack=[];this.redoStack=[];}snapshot(){return{vertices:cloneVertices(this.vertices),closed:this.closed,selection:[...this.selection]};}restore(s,{emit=true}={}){this.vertices=cloneVertices(s.vertices??[]);this.closed=Boolean(s.closed);this.selection=new Set(s.selection??[]);if(emit){this.emit('geometryChanged',{polygon:this.toPolygon()});this.emit('selectionChanged',{selection:[...this.selection]});}return this;}mutate(fn,event='geometryChanged'){const previous=this.snapshot();fn();this.undoStack.push(previous);this.redoStack.length=0;this.emit(event,event==='selectionChanged'?{selection:[...this.selection]}:{polygon:this.toPolygon()});return this;}addVertex(value,index=this.vertices.length){const vertex=normalizeVertex(value,`vertex-${++this.sequence}`);if(this.vertices.some(v=>v.id===vertex.id))throw new Error(`Polygon vertex id already exists: ${vertex.id}`);index=Math.max(0,Math.min(this.vertices.length,Math.floor(Number(index))));this.mutate(()=>this.vertices.splice(index,0,vertex));return vertex;}vertex(value){const vertex=typeof value==='string'?this.vertices.find(v=>v.id===value):value;if(!vertex||!this.vertices.includes(vertex))throw new Error('Unknown polygon vertex');return vertex;}moveVertex(value,next){const vertex=this.vertex(value);this.mutate(()=>{vertex.x=finite(next?.x??vertex.x,'Vertex x');vertex.z=finite(next?.z??vertex.z,'Vertex z');});return vertex;}removeVertex(value){const vertex=this.vertex(value),index=this.vertices.indexOf(vertex);this.mutate(()=>{this.vertices.splice(index,1);this.selection.delete(vertex.id);if(this.vertices.length<3)this.closed=false;});return vertex;}insertVertex(after,value){return this.addVertex(value,this.vertices.indexOf(this.vertex(after))+1);}closePolygon(){if(this.closed)return this;if(this.vertices.length<3)throw new Error('Polygon requires at least three vertices to close');this.mutate(()=>{this.closed=true;},'polygonClosed');this.emit('geometryChanged',{polygon:this.toPolygon()});return this;}openPolygon(){if(!this.closed)return this;this.mutate(()=>{this.closed=false;},'polygonOpened');this.emit('geometryChanged',{polygon:this.toPolygon()});return this;}select(value,{additive=false}={}){const vertex=this.vertex(value),previous=this.snapshot();if(!additive)this.selection.clear();this.selection.add(vertex.id);this.undoStack.push(previous);this.redoStack.length=0;this.emit('selectionChanged',{selection:[...this.selection]});return this;}clearSelection(){if(!this.selection.size)return this;const previous=this.snapshot();this.selection.clear();this.undoStack.push(previous);this.redoStack.length=0;this.emit('selectionChanged',{selection:[]});return this;}undo(){const s=this.undoStack.pop();if(!s)return false;this.redoStack.push(this.snapshot());this.restore(s);return true;}redo(){const s=this.redoStack.pop();if(!s)return false;this.undoStack.push(this.snapshot());this.restore(s);return true;}toPolygon(){return{vertices:cloneVertices(this.vertices),closed:this.closed};}edges(){return polygonEdges(this);}draw(renderer,context={}){if(!renderer)return;const world=v=>{const b=this.worldPosition();return[b[0]+v.x,b[1]+this.y,b[2]+v.z];};for(const edge of this.edges())renderer.line?.(world(edge.start),world(edge.end),this.color,this,context);for(const vertex of this.vertices)renderer.point?.(world(vertex),[.045,.045,.045],this.selection.has(vertex.id)?this.selectedColor:this.color,this,context);}}
export { PolygonEditor, polygonEdges };
